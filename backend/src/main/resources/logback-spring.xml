<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="jmro-queue"/>

    <!-- ============================================ -->
    <!-- PROFILE: default (production) — JSON output  -->
    <!-- ============================================ -->
    <springProfile name="!dev">
        <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>request_id</includeMdcKeyName>
                <includeMdcKeyName>user_id</includeMdcKeyName>
                <includeMdcKeyName>endpoint</includeMdcKeyName>
                <includeMdcKeyName>duration_ms</includeMdcKeyName>
                <includeMdcKeyName>error_code</includeMdcKeyName>
                <customFields>{"service":"${appName}"}</customFields>
                <timeZone>UTC</timeZone>
                <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSS'Z'</timestampPattern>
            </encoder>
        </appender>

        <root level="${LOGGING_LEVEL_ROOT:-INFO}">
            <appender-ref ref="CONSOLE_JSON"/>
        </root>
    </springProfile>

    <!-- ============================================ -->
    <!-- PROFILE: dev — Human-readable console output -->
    <!-- ============================================ -->
    <springProfile name="dev">
        <appender name="CONSOLE_PLAIN" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{request_id:-}] - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="DEBUG">
            <appender-ref ref="CONSOLE_PLAIN"/>
        </root>
    </springProfile>

</configuration>
